const a = [4, 5, 8, 9]
const b = [3, 4, 5, 7]

const countries = ['Finland', 'Sweden', 'Norway']

/*1. Find a union b*/
let c = [...a, ...b]

let A = new Set(a)
let B = new Set(b)
let C = new Set(c)
console.log(C)
/*2. Find a intersection b*/
/*3. Find a with b*/
